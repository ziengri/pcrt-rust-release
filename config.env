# Shared native Rust service configuration.
# Every recorder, processor and uploader must use this same sessions root.
SESSIONS_DIR=/var/lib/pcrt/sessions
RESULT_QUEUE_DB=/var/lib/pcrt/sessions/outbox/results.sqlite
ZMQ_IPC_ENDPOINT=ipc:///run/pcrt/doors.sock
